#include <cinttypes>
#include <cstddef>

extern "C" [[gnu::section(".fastCode"), gnu::noinline]]
void* memcpy(void* dest, const void* src, std::size_t count)
{
#include "memcpy.impl"
}

extern "C" [[gnu::section(".fastCode"), gnu::noinline]]
void* memset(void* dest, uint8_t value, std::size_t count)
{
#include "memset.impl"
}
